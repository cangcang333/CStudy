
spzhu
20180601

This is C study test program


git command
1. change a directory to git
    git init
2. add a file, e.g
    git add README
3.commit a file
    git commit -m "README file" 
4.connect local git repository and remote git repository,e.g
    git remote add origin git@github.com:cangcang333/CStudy.git
5.first push
    git push -u origin master
6.after push
    git push origin master
7.generate ssh key
    ssh-keygen -t rsa -C "cangcang333@163.com"

    This will create .ssh directory and id_rsa, id_rsa.pub two files
8.update your local repository
    git pull origin master

9. rename a file
    git mv <old filename> <new filename>
and then commit and push


Other git commands:
    git status
    git diff README





THE MOST IMPORTANT IS ADD, COMMIT, PUSH, PULL
